# Genart

Small deterministic generative-art libraries and the tools used to explore them.

- [`rosa`](./rosa) — zero-dependency seeded flower avatars.
- [`bloomlab`](./bloomlab) — browser playground for Rosa.

Run `npm test`, `npm run build`, or `npm run dev` from this directory.
