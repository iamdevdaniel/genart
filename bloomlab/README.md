# Bloom Lab

The no-framework browser playground for the sibling [`rosa`](../rosa) package.

From the repository root:

```sh
npm run dev
```

Then open <http://localhost:4173/bloomlab/>. The tiny development server uses only Node built-ins.
